import { Box } from "@mui/material";
import ListPage from "./pages/ListPage";

function Product() {
    return (
        <Box pt={1}>
            <ListPage />
        </Box>
    );
}

export default Product;